*** Begin Patch
*** Add File: .github/copilot-instructions.md
+# Copilot / AI Agent Instructions — SmartPDR
+
+Purpose
+- Short, actionable guidance to help AI agents be productive in this repo quickly.
+
+Quick files to open
+- index.tsx (entry / mount point)
+- App.tsx
+- components/, services/, SmartPDR/ (feature code)
+- index.html, manifest.json (hosting / PWA hints)
+- __tests__/ (unit tests; includes mount test)
+
+Key patterns & examples
+- Export mountApp from index.tsx for testability and HMR.
+- Tests: Jest + react testing utilities; place tests under __tests__ and mock App as needed.
+- HMR: guard with import.meta.hot or module.hot; avoid side effects under NODE_ENV === 'test'.
+
+Build / test (repo note)
+- No package.json found at repo root. If missing, create it:
+  - npm init -y
+  - npm install --save-dev jest ts-jest @types/jest @testing-library/react @testing-library/jest-dom
+  - Add "test": "jest" and jest config: { "testEnvironment": "jsdom", "transform": { "^.+\\\\.(ts|tsx)$": "ts-jest" } }
+  - Run tests: npm test or npx jest __tests__/index.test.tsx -i
+
+When editing
+- Keep changes local to feature folders.
+- Add tests in the same style and folder.
+- Check services/ for external integrations and ask maintainers for credentials or sample payloads.
+
+If package.json or CI exist in a subfolder, point me to them and I will update these instructions with exact commands and environment notes.
+
*** End Patch